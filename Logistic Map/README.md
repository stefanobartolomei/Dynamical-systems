The folder contains a brief study of the Logistic map: $x_{n+1} = r x_n (1 - x_n)$, where $r \in \mathbb{R}^+$.

The code [LogisticMap.m]() simulate the dynamic for different values of $r$. In [LyapunovExp]() it's studied how the Lyapunov exponent depends on the parameter of the map $\lambda(r)$, which shows for what values of $r$ the evolution is chaotic (\lambda>0$); while in
